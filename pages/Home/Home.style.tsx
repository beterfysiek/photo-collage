import {StyleSheet} from 'react-native';
import Constants from '../../services/constants/constants';

export default StyleSheet.create({
  container: {
    flex: 1,
    height: Constants.screen.height,
    backgroundColor: Constants.colour.grey_10,
  },
  gesture: {
    flex: 1,
  },
});
